# Copy to client/.env.production.local when building for Vercel/Netlify (split hosting)
# Leave empty / omit when frontend is served by the same Render/Node server

# Your live API URL (no trailing slash)
VITE_API_URL=https://your-portfolio-api.onrender.com
